# Nixpacks configuration for Railway
# This project requires both Python and Node.js

[providers]
python = "3.11"
node = "18"

[phases.setup]
nixPkgs = ["...", "postgresql"]

[phases.install]
dependsOn = ["setup"]

[phases.build]
dependsOn = ["install"]

[start]
cmd = "npm run railway-serve"
