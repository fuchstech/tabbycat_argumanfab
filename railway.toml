# Railway Configuration for Tabbycat

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./bin/docker-run-honcho.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
